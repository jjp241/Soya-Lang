def by_value(x = int) -> None {
    x = x + 10;
}

a = 10;
by_value(a);
print(a); # 10

def by_reference(ref x = int) -> None {
    x = x + 10;
}

b = 10;
by_reference(b);
print(b); # 20

def by_default_value(x = 10) -> None {
    x = x + 10;
    print(x);
}

by_default_value(); # 20

# List are passed by reference:
def pass_list(l = [int]) -> None {
    if(len(l) == 0) {
        return;
    }
    l[0] = 10;
    return;
}

l = [1, 2, 3];
pass_list(l);
print(l[0]); # 10
